# بوت تداول ذكي

واجهة ويب عربية تعتمد على Next.js لعرض بوت تداول مدعوم بالذكاء الاصطناعي. التطبيق يقدم لوحة تعريفية أنيقة، محاكاة تفاعلية للأداء المتوقع، وتوصيات تداول فورية مبنية على نموذج محاكاة كمي مبسط.

## 🚀 البدء السريع

### المتطلبات

- Node.js 18 أو أحدث
- npm (مرفق مع Node.js)

### خطوات التشغيل محلياً

```bash
npm install
npm run dev
```

ثم انتقل إلى `http://localhost:3000` لمعاينة التجربة.

### بناء نسخة الإنتاج

```bash
npm run build
npm start
```

## 🧠 ما الذي يقدمه التطبيق؟

- صفحة رئيسية تروّج للبوت مع شرح عربي كامل.
- مكوّن محاكاة يتيح تعديل رأس المال، مدة الاستثمار، مستوى المخاطر، ونوع الاستراتيجية، مع عرض مؤشرات الأداء الرئيسة (العائد المتوقع، معامل شارب، أقصى تراجع، إلخ).
- توليد توصيات تداول (شراء/بيع) مع مستويات دخول، وقف خسارة، وجني أرباح محسوبة حسابياً.
- تصميم بصري مستوحى من لوحات التداول الحديثة باستخدام Tailwind CSS.

## 🗂️ هيكل المشروع

```
app/                 ملفات Next.js (App Router)
├─ layout.tsx        تهيئة الخطوط والميتا والنسق العام
├─ page.tsx          الصفحة الرئيسية مع الهيكل العام
├─ globals.css       الأنماط العامة وتهيئة Tailwind
components/
└─ TradingSimulator.tsx  مكوّن المحاكاة التفاعلية
lib/
└─ simulation.ts         منطق المحاكاة وتوليد الإشارات
```

## 🛠️ التقنيات المستخدمة

- Next.js 14 (App Router)
- React 18 مع TypeScript
- Tailwind CSS للتصميم
- Vercel للنشر السحابي

## 📦 أوامر npm المتاحة

- `npm run dev` – تشغيل بيئة التطوير
- `npm run build` – إنشاء نسخة الإنتاج
- `npm start` – تشغيل الخادم بعد عملية البناء
- `npm run lint` – التحقق من معايير الكود باستخدام ESLint

## 🌐 النشر

تم النشر باستخدام Vercel عبر الأمر:

```bash
vercel deploy --prod --yes --token $VERCEL_TOKEN --name agentic-39e6881a
```

يمكن الوصول إلى النسخة المنتجة عبر: https://agentic-39e6881a.vercel.app

## ⚠️ ملاحظة أمنية

الإصدار الحالي من Next.js المستخدَم (`14.2.3`) مُشار إليه بتحذير أمني من Vercel. يُنصح بترقيته إلى إصدار مُحدَّث حال توفره.
